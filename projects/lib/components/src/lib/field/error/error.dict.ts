export const nameStr = '{friendlyName}'
export const valueStr = '{value}'

export const ErrorMessages = {
    required: `${nameStr} is required`,
    minLength: `${nameStr} needs to be more than ${valueStr} characters`,
    maxLength: `${nameStr} needs to be less than ${valueStr} characters`
};
